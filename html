<!doctype html>
<html lang="pt-BR">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Checkpoint | Mariana e Julia</title>
  <meta name="description" content="Arquivo de verificação para o Checkpoint das alunas Mariana e Julia.">
  <style>
    :root { --radius: 16px; }
    html, body { height: 100%; margin: 0; }
    body {
      font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Oxygen, Ubuntu, Cantarell, "Helvetica Neue", Arial, "Noto Sans", "Apple Color Emoji", "Segoe UI Emoji", "Segoe UI Symbol", sans-serif;
      display: grid;
      place-items: center;
      background: linear-gradient(135deg, #e0f2ff, #f6f7f9);
    }
    main {
      max-width: 900px;
      padding: 48px 40px;
      border-radius: var(--radius);
      background: #fff;
      box-shadow: 0 10px 25px rgba(0,0,0,.08);
      text-align: center;
    }
    h1 {
      font-size: clamp(22px, 4vw, 32px);
      line-height: 1.25;
      margin: 0 0 8px;
      font-weight: 700;
      letter-spacing: .2px;
      color: #0066cc;
    }
    p {
      margin: 0;
      color: #596273;
      font-size: 14px;
    }
  </style>
</head>
<body>
  <main>
    <h1><strong>esse é um arquivo guardado no buket para o Checkpoint das alunas Mariana e Julia</strong></h1>
    <p>Upload este arquivo no bucket S3 para comprovação do checkpoint.</p>
  </main>
</body>
</html>
